apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocp-v-templates.fullname" . }}-test-connection"
  labels:
    {{- include "ocp-v-templates.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocp-v-templates.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
